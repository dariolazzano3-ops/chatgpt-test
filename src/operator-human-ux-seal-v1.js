import { handleOperatorDashboard as handleDesignDashboard } from './operator-design-ux-v1.js';

const STYLE = String.raw`<style id="aurentara-human-ux-reality-v1-style">
.small{font-size:12px;line-height:1.5}.row-main strong{font-size:13.5px}.human-meta{font-size:11px;line-height:1.45;color:var(--muted);margin-top:3px}.human-meta code{font-size:10.5px;word-break:break-word}.human-section{display:grid;gap:14px}.human-head{display:flex;align-items:flex-end;justify-content:space-between;gap:14px}.human-head h2{margin:0;font-size:15px}.human-head p{margin:4px 0 0;color:var(--muted);font-size:12px}.human-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:9px}.human-kv{border:1px solid #eceee8;background:#fbfbf9;border-radius:10px;padding:10px 11px;min-width:0}.human-kv b{display:block;font-size:10px;letter-spacing:.045em;text-transform:uppercase;color:#666b64;margin-bottom:4px}.human-kv span{display:block;font-size:12.5px;line-height:1.4;overflow-wrap:anywhere}.human-summary{border:1px solid var(--line);border-radius:14px;background:var(--panel);padding:18px;box-shadow:var(--shadow)}.human-summary h3{margin:0;font-size:15px}.human-raw{margin-top:12px;padding-top:8px;border-top:1px solid var(--soft)}.human-raw summary{cursor:pointer;min-height:34px;font-size:11.5px;font-weight:650;color:#5f635d}.human-raw pre{margin:8px 0 0;max-height:360px}.human-all-clear{display:flex;align-items:center;justify-content:space-between;gap:16px;border:1px solid #dbe8de;background:#f5f9f6;border-radius:13px;padding:14px 16px}.human-all-clear strong{font-size:13.5px;color:#34533e}.human-empty{border:1px dashed #d4d8d1;background:#fafbf8;border-radius:12px;padding:14px 16px;color:#686d65;font-size:12px;line-height:1.5}.human-create{border:1px solid var(--line);border-radius:13px;background:#fff;margin-top:14px;overflow:hidden}.human-create>summary{cursor:pointer;list-style:none;padding:13px 16px;font-size:12.5px;font-weight:680;display:flex;justify-content:space-between}.human-create>summary::-webkit-details-marker{display:none}.human-create>summary:after{content:'+';font-size:17px;color:var(--muted)}.human-create[open]>summary:after{content:'−'}.human-create>[data-project-create],.human-create>.card{margin:0;border:0;border-top:1px solid var(--line);border-radius:0;box-shadow:none}.human-cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(270px,1fr));gap:12px}.human-card{border:1px solid var(--line);border-radius:13px;background:#fff;padding:15px 16px;min-width:0}.human-card h3{margin:0;font-size:14px}.human-note{margin-top:9px;border:1px solid var(--soft);background:#fafbf8;border-radius:9px;padding:9px 10px;color:#60645d;font-size:11.5px;line-height:1.5}.human-health{border-left:4px solid #66766a}.human-health.attention{border-left-color:#b78023}.human-health.blocked{border-left-color:#b34b4b}.human-blocker{border:1px solid #ead6c2;background:#fffaf4;border-radius:12px;padding:13px 14px}.human-blocker h3{margin:0 0 8px;font-size:13px}.human-safety-clear{padding:10px 12px!important;opacity:.88}.human-safety-clear:before{content:'Sicherheitsrahmen: alles klar';display:block;font-size:12px;font-weight:680;color:#3f6049;margin-bottom:6px}.human-safety-risk{border-color:#dfc2a4!important;background:#fff9f1!important}.human-policy .card{max-width:1100px}.human-policy dl{grid-template-columns:minmax(180px,.5fr) 1fr}.human-policy dt{font-size:11.5px}.human-policy dd{font-size:12.5px}#health .human-grid{grid-template-columns:repeat(4,minmax(180px,1fr))}#deliveries .human-summary{max-width:none}#approvals .empty,#audit .empty{min-height:58px}.section{max-width:1500px}@media(max-width:920px){#health .human-grid{grid-template-columns:1fr 1fr}}@media(max-width:700px){.human-grid,#health .human-grid,.human-cards{grid-template-columns:1fr}.human-head,.human-all-clear{align-items:flex-start;flex-direction:column}.human-policy dl{grid-template-columns:1fr}}
</style>`;

const SCRIPT = String.raw`<script id="aurentara-human-ux-reality-v1-script">
(() => {
  if (window.__aurentaraHumanUxRealityV1) return;
  window.__aurentaraHumanUxRealityV1 = true;

  const COPY={hq:['HQ','Aktueller Betriebszustand und nächste relevante Aktionen.'],projects:['Projekte','Unternehmen und operative Projekte verwalten.'],missions:['Missionen','Missionen, Pläne und Ergebnisse nachvollziehen.'],mission:['Mission Studio','Missionen formulieren, planen und kontrolliert ausführen.'],approvals:['Freigaben','Offene Entscheidungen und Freigaben prüfen.'],deliveries:['Ergebnisse','Auslieferungen, Qualität und Evidence nachvollziehen.'],executions:['Ausführungen','Laufende und vergangene Ausführungen mit Evidence prüfen.'],factories:['Factories','Reifegrad, Verifikation und Einsatzbereitschaft verfolgen.'],capabilities:['Capabilities','Registrierte Fähigkeiten und reale Verfügbarkeit prüfen.'],providers:['Provider','Verfügbarkeit, Gates und Integrationsstatus prüfen.'],costs:['Kosten','Budget, Ausgaben und Reserven im Blick behalten.'],quality:['Qualität','Qualitätszustände und Validierung nachvollziehen.'],alerts:['Blocker / Hinweise','Handlungsbedarf, Auswirkung und nächsten Schritt erkennen.'],health:['Systemstatus','Runtime, Staging und Systembereitschaft überwachen.'],audit:['Aktivität','Wichtige Operator- und Systemereignisse nachvollziehen.'],settings:['Richtlinien','Aktive Runtime-, Budget- und Sicherheitsrichtlinien einsehen.']};
  const NAV={hq:'HQ',projects:'Projekte',missions:'Missionen',mission:'Mission Studio',approvals:'Freigaben',deliveries:'Ergebnisse',executions:'Ausführungen',factories:'Factories',capabilities:'Capabilities',providers:'Provider',costs:'Kosten',quality:'Qualität',alerts:'Blocker',health:'Systemstatus',audit:'Aktivität',settings:'Richtlinien'};
  const TXT={'Projects':'Projekte','Approvals':'Freigaben','Providers':'Provider','Costs':'Kosten','Deliverables':'Ergebnisse','Deliveries':'Ergebnisse','Executions':'Ausführungen','System Health':'Systemstatus','Activity':'Aktivität','Audit Log':'Aktivität','Settings':'Richtlinien','Quality':'Qualität','Attention required':'Handlungsbedarf','Current operations':'Aktive Vorgänge','System & control':'System & Steuerung','Financial / Control':'Kosten & Steuerung','Recent activity':'Letzte Aktivität','Pending approvals':'Offene Freigaben','Variable cost':'Variable Kosten','Critical alerts':'Kritische Hinweise','Operator state':'Operator-Zustand','Provider attention':'Provider-Hinweise','Open':'Öffnen','Review':'Prüfen','Inspect':'Prüfen','Spent':'Ausgegeben','Reserved':'Reserviert','Estimated':'Geschätzt','Remaining':'Verbleibend','Default environment':'Standardumgebung','Data mode':'Datenmodus','Mission budget':'Missionsbudget','Monthly budget':'Monatsbudget','Retry limit':'Retry-Limit','Approval policy':'Freigaberichtlinie','Provider fallback':'Provider-Fallback','NOT CONFIGURED':'Nicht konfiguriert','Not configured':'Nicht konfiguriert'};
  const EVENTS={MISSION_PLAN_DURABLY_RECORDED:'Missionsplan gespeichert',SYNTHETIC_UNIVERSAL_MISSION_RECORDED:'Synthetische Mission abgeschlossen',UNIVERSAL_MISSION_RECORDED:'Mission aufgezeichnet',PROJECT_CREATED:'Projekt erstellt',PROJECT_UPDATED:'Projekt aktualisiert',MISSION_CREATED:'Mission erstellt',MISSION_PLANNED:'Mission geplant',MISSION_APPROVED:'Mission freigegeben',MISSION_REJECTED:'Mission abgelehnt',EXECUTION_STARTED:'Ausführung gestartet',EXECUTION_COMPLETED:'Ausführung abgeschlossen',EXECUTION_FAILED:'Ausführung fehlgeschlagen',DELIVERY_READY:'Ergebnis bereit',DELIVERY_COMPLETED:'Auslieferung abgeschlossen',APPROVAL_REQUESTED:'Freigabe angefordert',APPROVAL_APPROVED:'Freigabe erteilt',APPROVAL_REJECTED:'Freigabe abgelehnt'};
  const PROVIDER_STATES=new Set(['AVAILABLE','STAGING ONLY','CREDENTIAL REQUIRED','BUDGET GATE','PERMISSION GATE','UNAVAILABLE','PRODUCTION DISABLED','UNKNOWN']);
  const rows=v=>Array.isArray(v)?v:[];
  const first=(...v)=>v.find(x=>x!==null&&x!==undefined&&x!=='');
  const text=(v,fallback='Nicht verifiziert')=>v===null||v===undefined||v===''?fallback:String(v);
  const html=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const up=v=>String(v??'').trim().toUpperCase().replace(/-/g,'_');
  const statusTone=v=>{const s=up(v);if(['AVAILABLE','READY','COMPLETED','SUCCESS','HEALTHY','PASS','VERIFIED','STAGING_VERIFIED'].includes(s))return'ready';if(['ACTIVE','RUNNING','STAGING_ONLY'].includes(s))return'active';if(['BLOCKED','FAILED','FAILURE','UNAVAILABLE','ERROR'].includes(s))return'blocked';if(['CREDENTIAL_REQUIRED','BUDGET_GATE','PERMISSION_GATE','NOT_VERIFIED','UNKNOWN','ACTION_REQUIRED'].includes(s))return'attention';return'neutral'};
  const pill=v=>'<span class="badge '+statusTone(v)+'">'+html(text(v))+'</span>';
  const kv=(k,v)=>'<div class="human-kv"><b>'+html(k)+'</b><span>'+html(text(v))+'</span></div>';
  const money=v=>v===null||v===undefined||v===''?'Nicht verifiziert':(Number.isFinite(Number(v))?new Intl.NumberFormat('de-DE',{style:'currency',currency:'EUR'}).format(Number(v)):text(v));
  const bool=v=>v===true||up(v)==='TRUE'||up(v)==='ON'?'AN':v===false||up(v)==='FALSE'||up(v)==='OFF'?'AUS':text(v);

  function eventTitle(key){const raw=up(key);if(EVENTS[raw])return EVENTS[raw];if(!raw.includes('_'))return text(key);const phrase=raw.toLowerCase().split('_').filter(Boolean).join(' ');return phrase?phrase.charAt(0).toUpperCase()+phrase.slice(1):text(key)}
  window.aurentaraHumanEventTitleV1=eventTitle;
  function humanizeEvents(root){if(!root)return;root.querySelectorAll('strong,td').forEach(el=>{const raw=(el.textContent||'').trim();if(el.dataset.humanEvent==='true'||!/^[A-Z][A-Z0-9_]{3,}$/.test(raw)||!raw.includes('_'))return;el.dataset.humanEvent='true';el.innerHTML='<span>'+html(eventTitle(raw))+'</span><div class="human-meta"><code>'+html(raw)+'</code></div>'})}
  function translate(root){if(!root)return;root.querySelectorAll('h1,h2,h3,h4,strong,span,button,th,dt,label,summary').forEach(el=>{if(el.children.length)return;const raw=(el.textContent||'').trim();if(TXT[raw])el.textContent=TXT[raw]})}

  function chrome(id){document.querySelectorAll('.nav button[data-goto]').forEach(b=>{const label=b.querySelector('span:last-child');if(label&&NAV[b.dataset.goto])label.textContent=NAV[b.dataset.goto]});document.querySelectorAll('.nav-label').forEach(el=>{const m={Overview:'Übersicht',Work:'Arbeit',Operations:'Betrieb',Control:'Steuerung'};const raw=(el.textContent||'').trim();if(m[raw])el.textContent=m[raw]});const c=COPY[id]||['Operator Control','Privater operativer Leitstand.'];const title=document.getElementById('title');const sub=document.querySelector('.top .subtitle');if(title)title.textContent=c[0];if(sub)sub.textContent=c[1];const refresh=document.getElementById('refresh');if(refresh){const show=['health','providers','audit'].includes(id);refresh.hidden=!show;refresh.textContent='Aktualisieren';refresh.setAttribute('aria-hidden',show?'false':'true')}}

  function projects(){const root=document.getElementById('projects');if(!root)return;const form=root.querySelector('[data-project-create]');if(form&&!form.closest('.human-create')){const d=document.createElement('details');d.className='human-create';d.dataset.humanProjectCreate='true';const s=document.createElement('summary');s.textContent='Neues Projekt anlegen';form.parentNode.insertBefore(d,form);d.appendChild(s);d.appendChild(form)}translate(root)}

  function approvals(){const root=document.getElementById('approvals');if(!root)return;const f=state?.data?.functional||{};const a=f.approvals||state?.data?.approvals||{};const pending=Number(first(f.summary?.pending_approvals,a.pending_count,0));if(pending===0){const mission=rows(first(a.mission_plan,a.mission_plan_approvals,a.plans));const core=rows(first(a.core,a.core_approvals,a.items));root.innerHTML='<div class="human-all-clear"><div><strong>Keine Freigaben erforderlich</strong><div class="small">Es wartet aktuell keine Operator-Entscheidung.</div></div><div class="human-meta">Offene Freigaben: 0'+((mission.length||core.length)?' · Mission Plan: '+mission.length+' · Core: '+core.length:'')+'</div></div>';return}translate(root)}

  function providerState(p){let s=up(p?.status).replace(/_/g,' ');if(PROVIDER_STATES.has(s))return s;if(!s||s==='NOT VERIFIED'){if(p?.credentials_required===true)return'CREDENTIAL REQUIRED';return'UNKNOWN'}if(s.includes('CREDENTIAL'))return'CREDENTIAL REQUIRED';if(s.includes('BUDGET'))return'BUDGET GATE';if(s.includes('PERMISSION'))return'PERMISSION GATE';if(s.includes('UNAVAILABLE'))return'UNAVAILABLE';if(s.includes('STAGING'))return'STAGING ONLY';return'UNKNOWN'}
  function providerName(n){return({cloudflare:'Cloudflare',supabase:'Supabase',posthog:'PostHog',openai:'OpenAI','workers-ai':'Workers AI'})[String(n||'').toLowerCase()]||text(n)}
  function providers(){const root=document.getElementById('providers');if(!root)return;const items=rows(state?.data?.functional?.providers);root.innerHTML='<div class="human-section"><div class="human-head"><div><h2>Provider</h2><p>Registry, reale Verfügbarkeit und Gates. Fehlende Evidence bleibt sichtbar.</p></div></div>'+(items.length?'<div class="human-cards">'+items.map(p=>{const s=providerState(p);const extra=[];if(p.production_deploy===false||rows(p.current_restrictions).includes('PRODUCTION_DISABLED'))extra.push('PRODUCTION DISABLED');if(p.cost_mode==='paid_usage')extra.push('Bezahlte Nutzung benötigt Budgetfreigabe');return'<div class="human-card"><div class="row"><div><h3>'+html(providerName(p.name))+'</h3><div class="human-meta">'+html(text(p.category,'Registrierter Provider'))+'</div></div>'+pill(s)+'</div><div class="human-grid">'+kv('Umgebung',first(p.environment,p.inventory_registered?'Registry':null))+kv('Credentials',first(p.credentials_state,p.credentials_required===true?'Erforderlich':null))+kv('Health',p.health)+kv('Kostenmodus',p.cost_mode)+'</div><div class="human-note">'+html(extra.join(' · ')||'Keine weitere verifizierte Einschränkung.')+'</div></div>'}).join('')+'</div>':'<div class="human-empty">Keine Provider-Evidence in der bestehenden Registry verfügbar.</div>')+'</div>'}

  function factoryState(f){const raw=up(first(f.health,f.status));const ci=up(f.ci_verification);if(rows(f.open_blockers).length||raw.includes('BLOCK')||raw.includes('FAIL'))return'BLOCKED';if(raw.includes('PLANNED'))return'PLANNED';if(raw.includes('STAGING_VERIFIED')||ci==='VERIFIED'||ci==='PASS')return'STAGING VERIFIED';if(raw.includes('READY')&&Number(f.execution_count||0)>0)return'READY';if(f.factory)return'EXISTS';return'NOT VERIFIED'}
  function factories(){const root=document.getElementById('factories');if(!root)return;const items=rows(state?.data?.functional?.factories?.items);root.innerHTML='<div class="human-section"><div class="human-head"><div><h2>Factories</h2><p>Existenz, Verifikation und reale Run-Evidence klar getrennt.</p></div></div>'+(items.length?'<div class="human-cards">'+items.map(f=>{const s=factoryState(f),notes=[];if(!Number(f.execution_count||0))notes.push('Noch kein verifizierter Run');if(!rows(f.provider_availability).length)notes.push('Noch keine Provider-Evidence');if(f.quality_score===null||f.quality_score===undefined)notes.push('Qualität noch nicht gemessen');return'<div class="human-card"><div class="row"><div><h3>'+html(f.factory)+'</h3><div class="human-meta">'+html(text(f.role,''))+'</div></div>'+pill(s)+'</div><div class="human-grid">'+kv('Runs',f.execution_count)+kv('Erfolgsrate',f.success_rate_percent===null||f.success_rate_percent===undefined?'Nicht verifiziert':f.success_rate_percent+'%')+kv('Workload',f.current_workload===null?'Nicht verifiziert':f.current_workload)+kv('CI',f.ci_verification)+'</div><div class="human-note">'+html(notes.join(' · ')||'Verifizierte operative Evidence vorhanden.')+'</div></div>'}).join('')+'</div>':'<div class="human-empty">Keine Factories aus der bestehenden Core-Projektion verfügbar.</div>')+'</div>'}

  function deliveryRows(){return rows(first(state?.data?.deliveries?.universal_missions,state?.data?.deliveries?.items,state?.data?.functional?.deliveries))}
  function deliveryInfo(u){const p=u?.plan||u?.mission_plan||{},e=u?.execution||{},q=u?.quality||{},d=u?.delivery||{},s=u?.safety||{};const selected=rows(first(p.selected_capabilities,p.target_capabilities,u.selected_capabilities));const rejected=rows(first(p.rejected_capabilities,u.rejected_capabilities));const assumptions=rows(first(p.assumptions,u.assumptions));const factories=rows(first(u.factories,e.factories)).map(x=>typeof x==='string'?x:first(x.factory,x.name)).filter(Boolean);const providers=rows(first(u.providers,e.providers)).map(x=>typeof x==='string'?x:first(x.provider,x.name)).filter(Boolean);return{project:first(u.business_name,u.project,u.project_id,u.scope_key),mission:first(u.mission,u.mission_id,u.mission_type),status:first(u.status,e.status),final:first(u.final_delivery_status,d.status,u.delivery_status),quality:first(q.score,u.quality_score,q.status,u.quality_state),factory:first(e.factory,u.factory,factories.join(', ')),capability:first(u.capability,selected.join(', ')),provider:first(e.provider,u.provider,providers.join(', ')),cost:first(u.variable_cost_eur,u.actual_cost_eur,u.estimated_variable_cost_eur),environment:first(u.environment,e.environment,'staging'),production:first(s.production,u.production_deploy===false?'OFF':u.production_deploy),external:first(s.external_writes,u.external_writes===false?'OFF':u.external_writes),evidence:first(e.evidence,u.execution_evidence,e.execution_id),assumptions:assumptions.join(' · ')||'Keine verifizierten Annahmen ausgewiesen',rejected:rejected.join(', ')||'Keine ausgewiesen',reference:first(d.reference,d.artifact_ref,d.url,u.delivery_reference,u.result_reference,u.mission_id)}}
  function deliveries(){const root=document.getElementById('deliveries');if(!root)return;const items=deliveryRows();root.innerHTML='<div class="human-section"><div class="human-head"><div><h2>Unified Delivery</h2><p>Human Summary zuerst. Vollständige technische Evidence bleibt sekundär erhalten.</p></div></div>'+(items.length?items.map(u=>{const x=deliveryInfo(u);return'<article class="human-summary" data-human-unified-delivery="true"><div class="row"><div><h3>'+html(text(x.project,'Projekt'))+'</h3><div class="human-meta">Mission: '+html(text(x.mission))+'</div></div>'+pill(x.final)+'</div><div class="human-grid">'+kv('Projekt',x.project)+kv('Mission',x.mission)+kv('Status',x.status)+kv('Finaler Delivery-Status',x.final)+kv('Qualität',x.quality)+kv('Factory',x.factory)+kv('Capability',x.capability)+kv('Provider',x.provider)+kv('Kosten',x.cost===null||x.cost===undefined?'Nicht verifiziert':money(x.cost))+kv('Umgebung',x.environment)+kv('Production',bool(x.production))+kv('External Writes',bool(x.external))+kv('Execution Evidence',typeof x.evidence==='object'?JSON.stringify(x.evidence):x.evidence)+kv('Wichtige Annahmen',x.assumptions)+kv('Abgelehnte / ungenutzte Capabilities',x.rejected)+kv('Delivery / Result Reference',x.reference)+'</div><details class="human-raw"><summary>Technische Details / Raw Evidence</summary><pre>'+html(JSON.stringify(u,null,2))+'</pre></details></article>'}).join(''):'<div class="human-empty">Noch keine Unified-Delivery-Evidence vorhanden.</div>')+'</div>'}

  function health(){const root=document.getElementById('health');if(!root)return;const f=state?.data?.functional||{},sh=f.system_health||f.systemHealth||state?.data?.health||{},alerts=rows(f.alerts),blockers=alerts.filter(a=>['BLOCKED','FAILED','ACTION_REQUIRED'].includes(up(a.severity)));const runtime=first(sh.runtime_health,sh.runtime?.status,'Nicht verifiziert'),staging=first(sh.staging_verification,sh.staging?.status,sh.staging_status,'Nicht verifiziert'),activation=first(sh.activation_readiness,sh.activation?.status,sh.activation_status,'Nicht verifiziert');const dimensionUnknown=[runtime,staging,activation].some(v=>{const s=up(v);return s.includes('UNKNOWN')||s.includes('NOT_VERIFIED')||s.includes('NICHT VERIFIZIERT')});let overall='BETRIEBSBEREIT (STAGING)';if(blockers.length||['BLOCKED','FAILED'].includes(up(f.summary?.operator_state)))overall='HANDLUNGSBEDARF';else if(dimensionUnknown||up(f.summary?.operator_state)==='UNKNOWN')overall='NICHT VOLLSTÄNDIG VERIFIZIERT';const cls=overall==='HANDLUNGSBEDARF'?'blocked':overall.includes('NICHT')?'attention':'';root.innerHTML='<div class="human-section"><div class="human-summary human-health '+cls+'"><div class="human-head"><div><h2>Overall System State</h2><p>Runtime, Staging, Activation und Production werden getrennt bewertet.</p></div>'+pill(overall)+'</div><div class="human-grid">'+kv('Runtime Health',runtime)+kv('Staging Verification',staging)+kv('Activation Readiness',activation)+kv('Production State','DISABLED')+'</div></div>'+(blockers.length?blockers.map(b=>'<div class="human-blocker"><h3>'+html(text(b.what,'Blocker'))+'</h3><div class="human-grid">'+kv('Warum',b.why)+kv('Auswirkung',b.impact)+kv('Nächste Aktion',b.next_action)+'</div></div>').join(''):'<div class="human-all-clear"><div><strong>Kein aktiver Systemblocker</strong><div class="small">Nicht verifizierte Teilzustände bleiben oben separat sichtbar.</div></div></div>')+'</div>'}

  function projectDetail(){const id=String(state?.section||'');if(!id.includes('project'))return;const root=document.getElementById('project-detail')||document.getElementById('project')||document.querySelector('[data-project-detail]');if(!root)return;if(!root.querySelector('[data-human-project-priority]')){const p=state?.data?.project_detail||state?.data?.project||state?.data?.selected_project||{};const s=up(first(p.status,p.state));let next='Nächsten verifizierten Projektschritt prüfen';if(['BLOCKED','FAILED'].includes(s))next='Blocker und letzte Evidence prüfen';else if(['COMPLETED','DONE','SUCCESS'].includes(s))next='Keine unmittelbare Aktion erforderlich';else if(['ACTIVE','RUNNING'].includes(s))next='Aktuellen Missions- und Freigabestatus prüfen';else if(['CREATED','NEW'].includes(s))next='Mission definieren';const box=document.createElement('div');box.className='card human-section';box.dataset.humanProjectPriority='true';box.innerHTML='<div class="human-head"><div><h2>Projekt auf einen Blick</h2><p>Status, aktueller Zustand und nächste Aktion zuerst.</p></div>'+pill(first(p.status,p.state))+'</div><div class="human-grid">'+kv('Projektstatus',first(p.status,p.state))+kv('Aktueller Zustand',first(p.current_state,p.phase,p.current_phase))+kv('Capabilities',rows(first(p.capabilities,p.capability_map)).length||'Nicht verifiziert')+kv('Ergebnisse',rows(first(p.results,p.deliverables)).length||'Noch kein Ergebnis')+kv('Nächste Aktion',next)+'</div>';root.prepend(box)}humanizeEvents(root)}

  function policies(){const root=document.getElementById('settings');if(!root)return;root.classList.add('human-policy');translate(root);root.querySelectorAll('dd,span,strong').forEach(el=>{if((el.textContent||'').trim()==='NOT CONFIGURED')el.textContent='Nicht konfiguriert'})}
  function safety(){const root=document.getElementById('mission');if(!root)return;const panel=root.querySelector('.functional-safety-panel');if(!panel)return;panel.classList.remove('human-safety-clear','human-safety-risk');const t=up(panel.textContent);const risky=/BLOCKED|FAILED|RISK|REQUIRED|EXTERNAL WRITE: ON|PRODUCTION: ON/.test(t);panel.classList.add(risky?'human-safety-risk':'human-safety-clear')}
  function polish(id){chrome(id);if(id==='projects')projects();if(id==='approvals')approvals();if(id==='providers')providers();if(id==='factories')factories();if(id==='deliveries')deliveries();if(id==='health')health();if(id==='settings')policies();if(id==='mission')safety();if(String(id).includes('project'))projectDetail();const active=document.getElementById(id);translate(active);humanizeEvents(active);document.documentElement.dataset.humanUxSeal='reality-fix-v1'}

  if(typeof render==='function'){const previous=render;render=function(id){previous(id);requestAnimationFrame(()=>polish(id))}}
  requestAnimationFrame(()=>polish(state?.section||'hq'));
})();
</script>`;

export async function handleOperatorDashboard(request, env = {}, ctx = {}, options = {}) {
  const response = await handleDesignDashboard(request, env, ctx, options);
  if (!response) return null;
  const url = new URL(request.url);
  const type = response.headers.get('content-type') || '';
  if (!(url.pathname === '/operator' || url.pathname === '/operator/') || response.status !== 200 || !type.includes('text/html')) return response;
  const source = await response.text();
  let body = source.includes('</head>') ? source.replace('</head>', `${STYLE}</head>`) : `${STYLE}${source}`;
  body = body.includes('</body>') ? body.replace('</body>', `${SCRIPT}</body>`) : `${body}${SCRIPT}`;
  const headers = new Headers(response.headers);
  headers.set('x-aurentara-human-ux-seal', 'reality-fix-v1');
  return new Response(body, { status: response.status, statusText: response.statusText, headers });
}

export function operatorHumanUxSealManifest() {
  return {
    schema: 'aurentara.operator-human-ux-seal.v1',
    presentation_only: true,
    canonical_route: '/operator',
    same_control_plane: true,
    existing_design_layer_reused: true,
    core_logic_changed: false,
    api_contract_changed: false,
    human_summary_first: true,
    raw_evidence_preserved_secondary: true,
    canonical_human_language: 'de',
    project_creation_secondary: true,
    human_event_titles_first: true,
    provider_truth_fail_closed: true,
    factory_truth_fail_closed: true,
    health_dimensions_separated: true,
    settings_presented_as_read_only_policies: true,
    production_deploy: false,
    external_writes: false,
    real_customer_data: false,
    additional_variable_cost_eur: 0
  };
}
