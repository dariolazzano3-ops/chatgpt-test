# Mission State

Durable RIOSYSTEMS orchestration missions are stored here as JSON files.

LEAN V4.0 supervisors may update mission task state and outputs, but may not trigger production deployment. Production remains a separate manually approved release path.
