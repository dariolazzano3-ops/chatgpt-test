name: Visual Foundry AURENTARA Attention Header Local

on:
  push:
    branches:
      - factory/visual-foundry-v1
    paths:
      - '.github/workflows/visual-foundry-aurentara-attention-header-local.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  attention-header-local:
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: A0_CONTROL
            title_size: 13
            title_weight: 650
            subtitle_size: 8.5
          - id: H5_LOCAL
            title_size: 15
            title_weight: 700
            subtitle_size: 10
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      GOLD_STANDARD_RUN: 1
      VISUAL_FOUNDRY_PANEL_HEADER_TYPOGRAPHY_CANDIDATE: ${{ matrix.id }}
      VISUAL_FOUNDRY_PANEL_HEADER_SCOPE: ATTENTION
      VISUAL_FOUNDRY_PANEL_HEADER_TITLE_SIZE_PX: ${{ matrix.title_size }}
      VISUAL_FOUNDRY_PANEL_HEADER_TITLE_WEIGHT: ${{ matrix.title_weight }}
      VISUAL_FOUNDRY_PANEL_HEADER_SUBTITLE_SIZE_PX: ${{ matrix.subtitle_size }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-package-lock
      - run: npx playwright install --with-deps chromium
      - run: node --check scripts/visual-foundry-gold-standard-isolated-run.mjs
      - run: node scripts/visual-foundry-gold-standard-isolated-run.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: aurentara-attention-header-${{ matrix.id }}
          path: artifacts/visual-foundry/gold-standard/run-1
          if-no-files-found: error
