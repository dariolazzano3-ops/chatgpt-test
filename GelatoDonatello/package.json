{
  "name": "gelatodonatello",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/frozen.js && node --check src/header-refresh-v1.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
