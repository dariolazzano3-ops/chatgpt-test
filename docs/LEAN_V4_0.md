# LEAN V4.0

V4.0 connects durable orchestration missions to the existing Web Factory through a supervised, explicitly triggered executor.

Mission task → execution contract → authorized web adapter → Factory Control request → Preview/QA/Self-Healing → durable Factory job → reconciliation into the same mission.

Production deploy remains disabled. Production continues through the separate manual release workflow only.
