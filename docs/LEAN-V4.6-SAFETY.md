# LEAN V4.6 Safety Invariants

The release keeps production deployment disabled, requires explicit adapter dispatch authorization, keeps automatic adapter dispatch disabled, keeps automatic multi-factory execution disabled, and delegates external Automation action safety to the existing V4.3-V4.5 guards.
